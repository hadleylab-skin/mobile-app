import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        position: 'relative',
    },
    activityIndicator: {
        position: 'absolute',
        top: 10,
        left: 0,
        right: 0,
        justifyContent: 'center',
        zIndex: 1,
    },
});
