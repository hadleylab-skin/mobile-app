import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 20,
    },
    moles: {
        marginBottom: 22,
    },
    text: {
        color: '#000',
        fontSize: 16,
        lineHeight: 20,
    },
});
