import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 64,
        position: 'relative',
    },
    activityIndicator: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: 100,
        alignItems: 'center',
    },
});
